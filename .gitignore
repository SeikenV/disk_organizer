# Rust build output
/target

# Cloned upstream projects kept locally for reference only (not vendored into git)
/references
/ollama-rs

# Project-managed tools fetched by scripts/setup_tools.ps1 (llama.cpp binaries + GGUF models)
/tools

# Runtime logs and probe/analysis outputs
/logs
scripts/last_run_setup.log
enrichment_probe_log.json
analysis_result.json
video_analysis_result.json
predictions_*.json
enrichment_report_*.log

# Local scan snapshots / output (contain personal disk listings)
*.snapshot.json
scan_output.txt
*.scan.txt

# External UI app build artifacts (separate toolchain, not part of the cargo workspace)
/ui/node_modules
/ui/dist
/ui/build
/ui/.expo
/ui/android/app/build
/ui/ios/build
