# 2026-06-30

## setup_llama_cpp ↔ test_llm 对接检查与修复

- 检查了 `scripts/setup_llama_cpp.ps1` 和 `scripts/test_llm.ps1` 的对接情况。
- 路径一致：`tools/llamacpp/<backend>/llama-server.exe` 和 `tools/models/*.gguf` 两端匹配正确。
- 发现 `test_llm.ps1` 缺少 pre-flight 检查，不会在环境未就绪时给出明确提示。
- 新增 pre-flight 检查：启动前验证 llama-server 二进制和模型文件是否存在，缺失则明确引导用户先跑 `setup_llama_cpp.bat`。
- 同时改进了 snapshot 缺失时的错误提示，补充了 setup 引导。
- 引擎不使用 setup 生成的 INI 预设文件（models-*.ini），这些只被 benchmark.ps1 使用——属无害多余工作。
